# ----------------------------------------------------------------------------
# Keeping Eyes on Your Agents - AV Data Hack Summit 2026
# Travel-planner agentic demo: LangGraph + MCP + DeepEval + Obot
# Python 3.11+ recommended
# ----------------------------------------------------------------------------

# --- Multi-agent orchestration ---
langgraph>=0.2.50
langchain>=0.3.7
langchain-core>=0.3.15
langchain-openai>=0.2.8

# --- MCP (Model Context Protocol) ---
# Official Python SDK ships FastMCP (mcp.server.fastmcp) used by the mock servers
mcp>=1.4.0
# Bridges MCP tools into LangChain/LangGraph
langchain-mcp-adapters>=0.1.0

# --- LLM provider ---
openai>=1.55.0

# --- Evaluation + Observability (Layers 1-3) ---
deepeval>=2.0.0

# --- Web backend (serves the Next.js UI via SSE) ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0

# --- Optional lightweight UI (fallback) ---
streamlit>=1.39.0

# --- Plumbing ---
python-dotenv>=1.0.1
pydantic>=2.9.0
httpx>=0.27.0
rich>=13.9.0
